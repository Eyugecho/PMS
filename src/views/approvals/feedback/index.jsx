import {Grid, Typography } from '@mui/material';
import React from 'react';

export const Feedbacks = () => {
  return (
    <Grid>
      <Grid item={12}>
        <Typography variant="subtitle1">Feedbacks page</Typography>
      </Grid>
    </Grid>
  );
};

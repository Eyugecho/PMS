import {Grid, Typography } from '@mui/material';
import React from 'react';

export const DailyActivity = () => {
  return (
    <Grid>
      <Grid item={12}>
        <Typography variant="subtitle1">DailyActivity</Typography>
      </Grid>
    </Grid>
  );
};
